PORT=3000
JWT_SECRET=replace-with-strong-secret

# Optional: enable PostgreSQL instead of demo memory mode
# DATABASE_URL=postgresql://user:password@localhost:5432/construction_erp
# DATABASE_SSL=false

# Demo mode works without a database. Set false in production.
DEMO_MODE=true
